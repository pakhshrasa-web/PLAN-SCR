name: Build Android APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 45
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4  # به‌روزرسانی به v4
    
    - name: Setup Python
      uses: actions/setup-python@v5  # به‌روزرسانی به v5
      with:
        python-version: '3.10'
    
    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          git zip unzip openjdk-17-jdk \
          autoconf libtool pkg-config zlib1g-dev \
          libncurses5-dev libncursesw5-dev libtinfo5 \
          cmake libffi-dev libssl-dev \
          python3-pip
        sudo apt-get clean
    
    - name: Install Python dependencies
      run: |
        pip install --upgrade pip
        pip install buildozer cython virtualenv
    
    - name: Build APK
      run: |
        buildozer android release
    
    - name: Upload APK
      uses: actions/upload-artifact@v4  # 🔥 مهم: به‌روزرسانی به v4
      with:
        name: app-release
        path: bin/*.apk
        retention-days: 30
        compression-level: 0  # برای افزایش سرعت آپلود
        if-no-files-found: error  # اگر فایل پیدا نشد، خطا بده
